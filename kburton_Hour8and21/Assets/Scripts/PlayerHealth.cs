using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerHealth : MonoBehaviour
{
    int healthpoints = 3992
    void Start()
    {
        UsePotion(healthpoints);    
    }

    // Update is called once per frame
    void Update()
    int UsePotion(health)
    {
        return health + 400;
    }
}
